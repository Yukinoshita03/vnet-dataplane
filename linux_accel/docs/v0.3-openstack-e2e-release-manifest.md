# v0.3 OpenStack E2E Release Manifest

## Release identity

| Field | Value |
| --- | --- |
| Intended tag | `v0.3-openstack-e2e` |
| Merge source | `feature/openstack-dns-e2e` |
| Merge base | `origin/main` / `b5e6e338ad10c1f11221af11558b5c52d95eeeb1` |
| Collected at | 2026-07-30 (Asia/Shanghai) |
| Scope | `linux_accel` only; unrelated root worktree files are excluded |
| Formal DNS artifact | `/tmp/vnet-p0-dns-formal-20260730-115435` on `master` |
| Final hook regression artifact | `/tmp/vnet-p0-safety-5cf20cc/hook-ownership-final` on `master` |

This is a mutable-lab inventory. Resolve server IDs, Neutron ports, OVS
interfaces, ifindexes, and security-group state again before a future run.

## Lab and instance inventory

| Component | Verified value |
| --- | --- |
| Controller / current compute | `master`, `192.168.106.130`, Linux `7.0.0-28-generic` |
| Additional computes | `compute2` (`192.168.122.50`), `compute3` (`192.168.122.51`) |
| Nova state | three QEMU computes and Nova compute services `enabled/up` at collection |
| Tenant network | `private`, ID `9878347d-d271-46f7-8bac-2914a21fe461` |
| Image | `ubuntu-24.04-noble-cloud`, ID `50998ffb-8ce8-4541-a3e5-d933f890f917` |
| Flavor | `ds2G`: 2 vCPU, 2048 MiB RAM, 10 GiB disk |
| Client | `private-p7-dns-path-r19-client`, ID `5d983776-e49c-4320-b038-f720df01ace6`, port `2200c160-c07d-43ea-9cda-5aabd4e54d37`, `10.0.0.43`, `ACTIVE`, host `master` |
| Backend | `private-p7-dns-path-r19-backend`, ID `383d55a1-2d78-4ec2-927e-0575312ccc0d`, port `8ddb7d32-92ab-41e2-b5b9-94596bef3952`, `10.0.0.55`, `ACTIVE`, host `master` |
| Guest keypair | `codex-grpc-ubuntu` |
| Client host path | `tap2200c160-c0`, ifindex `29` at collection |
| Existing foreign TC | NetMig ingress `0x65` / program ID `57`; egress `0x66` / program ID `56` |

The VMware environment is a nested QEMU/KVM lab. It supports the recorded
OpenStack networking experiment, but is not evidence of real NVIDIA vGPU
migration.

## Formal DNS E2E

The formal run reused the two listed instances; it did not create or delete a
server. Its effective client mode was `host-ebpf`, despite requesting
`GUEST_BPF=1`: the client XDP/TC monitor ran on `tap2200c160-c0`, while the
server generic-XDP monitor ran on guest `ens3`.

```bash
CLIENT_SERVER=5d983776-e49c-4320-b038-f720df01ace6 \
BACKEND_SERVER=383d55a1-2d78-4ec2-927e-0575312ccc0d \
GUEST_KEY=<private-key-for-codex-grpc-ubuntu> \
NETNS=codex-campaign-probe USE_FLOATING_IP=0 \
GUEST_BPF=1 REQUIRE_TC_COEXISTENCE=1 \
REQUESTS=1000 WARMUP=100 REPEAT=5 SUDO_PASS=<lab-secret> \
bash ./bench/openstack_dns_e2e.sh
```

| Scenario | QPS min | QPS median | QPS max | Relative median | p95 median (us) | p99 median (us) | Backend requests/run |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| baseline | 1797.85 | 2081.12 | 2393.69 | 1.00x | 619.92 | 965.68 | 1100 |
| monitor-only | 303.75 | 2082.19 | 2233.31 | 1.00x | 667.26 | 948.81 | 1100 |
| server-only | 2447.43 | 2678.64 | 2771.37 | 1.29x | 480.30 | 868.24 | 0 |
| client-only | 4502.68 | 9859.51 | 11796.63 | 4.74x | 135.30 | 222.71 | 1 |
| both | 9401.96 | 10208.86 | 10802.01 | 4.91x | 134.78 | 213.52 | 0 |

All 25 main scenarios reported `success=1000 failed=0`. Every client-only
round recorded `cache_learned=1 cache_hit=1099 cache_tx=1099`; every both
round had one client learning request, zero backend requests, and server
`cache_hit=1 cache_tx=1`. TTL expiry, untrusted resolver, and NXDOMAIN checks
each produced the expected two backend requests. `monitor-only` had one
host-side long-tail outlier in run 5 (p99 `28647.43 us`); it is retained in
the median calculation and is not used as an acceleration claim.

## Candidate source and validation

The formal artifact contains the exact SHA-256 snapshot for the code used by
the run:

```text
167b49010f8ef8858ed18bdea48ed662755889f2d6a8a78dc45a334c0c09b9f3  src/dns_monitor.cpp
1dfdbf91ee3542941f64f7d0c6f6d170aa08b74bc6f865fd9a9cb5c638ed25bd  src/grpc_monitor.cpp
8fd9ad632c8f195b39355fa6febf7d617439561de9a88a8a8d9686d2e7efc98d  agent/openstack_dataplane_agent.py
48a892396bbf4ee938b4a284e24b97234a1a99ad820a23d6e37bb3251997a08a  bench/openstack_dns_e2e.sh
aa3880b85e7213c0951bd8df63dc1586c000cb4a7c5ba3c8ce940ff521cf6349  tests/hook_ownership_regression_test.sh
8944aa892809258547e2d278f619dd9e191df1b6fa470c81ae7b739ac74447dd  tests/test_openstack_dataplane_agent.py
39122ad0de98e999df29285795b7a12f126d868006c2b4deaa1d798fbae476ae  scripts/build_linux.sh
```

After the formal E2E run, the isolated hook-ownership test was expanded to
replace both DNS and gRPC TC directions. This test-only change did not alter
the E2E runtime code or benchmark script. The final release validation on
`master` used this SHA-256 and passed:

```text
eafeef8e95e5955cf0d6b5d26e696a832f43e08a6c0368ccd2fbd7847b7872be  tests/hook_ownership_regression_test.sh
```

On `master`, the candidate passed:

```text
bash ./scripts/build_linux.sh
sudo bash ./tests/bpf_runtime_verifier_test.sh
sudo bash ./tests/dns_runtime_mode_integration_test.sh
sudo bash ./tests/grpc_runtime_mode_integration_test.sh
sudo bash ./tests/runtime_control_bpf_integration_test.sh
sudo bash ./tests/grpc_stream_correlation_test.sh
sudo bash ./tests/hook_ownership_regression_test.sh
```

The build included all binaries, TC coexistence, dynamic-controller,
runtime-control, and six Python Agent tests. The stream regression passed
HTTP/2 stream IDs `1,3,5`; all listed root runtime tests passed. The hook
regression creates only a fresh veth pair, proves normal owned cleanup,
foreign TC `0x65/0x66` preservation, XDP collision rejection, and
replacement preservation for both DNS and gRPC ingress/egress directions.

## Cleanup and temporary access change

- E2E recorded `cleanup_status=0`.
- After the run, no DNS/gRPC monitor process was present. `tap2200c160-c0`
  retained only NetMig TC `0x65/0x66`; no DNS `0x1`, gRPC `0x2`, or XDP
  attachment remained.
- The existing security group initially permitted SSH only from
  `172.24.4.0/24`, while `codex-campaign-probe` uses `10.0.0.40`. A temporary
  TCP/22 rule limited to `10.0.0.40/32` was created for the run, recorded as
  `2b8c3db6-0778-49e4-93cc-e46b56b5fc14`, then deleted and verified absent.
- Both reused instances remained `ACTIVE` after cleanup.

## Boundaries carried into P1

- The dynamic DNS/gRPC campaign's three-endpoint map write is serial
  experiment orchestration, not distributed atomic epoch publication.
- DNS XDP cleanup uses an atomic `old_prog_fd` precondition. Legacy
  `bpf_tc_*` detach remains slot-based, so its readback guard is fail-closed
  for observed ownership changes but cannot make an uncoordinated external
  replacement race atomic. P1 should add TCX BPF-link lifecycle where
  supported, with an explicit `BYPASS` fallback.
- The current Agent is a manual, single-port `watch` workflow. systemd,
  multi-port state/health reporting, migration freeze/recovery, and
  health-gated epoch publication are P1 acceptance work.
- gRPC fast response remains a userspace service path; this release does not
  claim universal in-kernel TCP/HTTP2 response synthesis.
