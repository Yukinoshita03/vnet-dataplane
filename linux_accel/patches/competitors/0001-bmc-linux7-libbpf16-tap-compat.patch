diff --git a/bmc/bmc_common.h b/bmc/bmc_common.h
index 3f8e8b5e54e919da040c61a64555b45345584cbf..fae247fda77045ab22046cdeb2660d387f34fe09 100644
--- a/bmc/bmc_common.h
+++ b/bmc/bmc_common.h
@@ -12,13 +12,23 @@
 #ifndef _BMC_COMMON_H
 #define _BMC_COMMON_H
 
+#ifndef BMC_MAX_KEY_LENGTH
 #define BMC_MAX_KEY_LENGTH 250
+#endif
+#ifndef BMC_MAX_VAL_LENGTH
 #define BMC_MAX_VAL_LENGTH 1000
+#endif
 #define BMC_MAX_ADDITIONAL_PAYLOAD_BYTES 53
 #define BMC_MAX_CACHE_DATA_SIZE BMC_MAX_KEY_LENGTH+BMC_MAX_VAL_LENGTH+BMC_MAX_ADDITIONAL_PAYLOAD_BYTES
+#ifndef BMC_MAX_KEY_IN_MULTIGET
 #define BMC_MAX_KEY_IN_MULTIGET 30
+#endif
+#ifndef BMC_CACHE_ENTRY_COUNT
 #define BMC_CACHE_ENTRY_COUNT 3250000
+#endif
+#ifndef BMC_MAX_PACKET_LENGTH
 #define BMC_MAX_PACKET_LENGTH 1500
+#endif
 #define BMC_MAX_KEY_IN_PACKET BMC_MAX_KEY_IN_MULTIGET
 
 #define FNV_OFFSET_BASIS_32		2166136261
diff --git a/bmc/bmc_kern.c b/bmc/bmc_kern.c
index 5a2f064019ac7b8494efa506dd280e35fa77c6a1..8f153f3ca74559d8023d4b1a0772f06c8e7d6d55 100644
--- a/bmc/bmc_kern.c
+++ b/bmc/bmc_kern.c
@@ -14,15 +14,20 @@
 #include <linux/ip.h>
 #include <linux/udp.h>
 #include <linux/tcp.h>
+#include <linux/pkt_cls.h>
 #include "bpf_helpers.h"
+#include <bpf_endian.h>
 
 #include "bmc_common.h"
 
-#define ADJUST_HEAD_LEN 128
+/* 48 bytes cover the configured response's measured 46-byte growth. */
+#define ADJUST_HEAD_LEN 48
+#define IPPROTO_TCP 6
+#define IPPROTO_UDP 17
+#define htons bpf_htons
 
-#ifndef memmove
-# define memmove(dest, src, n)  __builtin_memmove((dest), (src), (n))
-#endif
+#define memmove(dest, src, n) __builtin_memmove((dest), (src), (n))
+#define memcpy(dest, src, n) __builtin_memcpy((dest), (src), (n))
 
 struct memcached_udp_header {
     __be16 request_id;
@@ -40,7 +45,7 @@ struct memcached_udp_header {
 /* cache */
 struct {
 	__uint(type, BPF_MAP_TYPE_ARRAY);
-	__type(key, u32);
+	__type(key, __u32);
 	__type(value, struct bmc_cache_entry);
 	__uint(max_entries, BMC_CACHE_ENTRY_COUNT);
 } map_kcache SEC(".maps");
@@ -48,7 +53,7 @@ struct {
 
 /* keys */
 struct memcached_key {
-	u32 hash;
+	__u32 hash;
 	char data[BMC_MAX_KEY_LENGTH];
 	unsigned int len;
 };
@@ -67,41 +72,41 @@ struct parsing_context {
 	unsigned short read_pkt_offset;
 	unsigned short write_pkt_offset;
 };
-struct bpf_map_def SEC("maps") map_parsing_context = {
-	.type = BPF_MAP_TYPE_PERCPU_ARRAY,
-	.key_size = sizeof(unsigned int),
-	.value_size = sizeof(struct parsing_context),
-	.max_entries = 1,
-};
+struct {
+	__uint(type, BPF_MAP_TYPE_PERCPU_ARRAY);
+	__type(key, unsigned int);
+	__type(value, struct parsing_context);
+	__uint(max_entries, 1);
+} map_parsing_context SEC(".maps");
 
 /* stats */
-struct bpf_map_def SEC("maps") map_stats = {
-	.type = BPF_MAP_TYPE_PERCPU_ARRAY,
-	.key_size = sizeof(unsigned int),
-	.value_size = sizeof(struct bmc_stats),
-	.max_entries = 1,
-};
+struct {
+	__uint(type, BPF_MAP_TYPE_PERCPU_ARRAY);
+	__type(key, unsigned int);
+	__type(value, struct bmc_stats);
+	__uint(max_entries, 1);
+} map_stats SEC(".maps");
 
 /* program maps */
-struct bpf_map_def SEC("maps") map_progs_xdp = {
-	.type = BPF_MAP_TYPE_PROG_ARRAY,
-	.key_size = sizeof(u32),
-	.value_size = sizeof(u32),
-	.max_entries = BMC_PROG_XDP_MAX,
-};
+struct {
+	__uint(type, BPF_MAP_TYPE_PROG_ARRAY);
+	__type(key, __u32);
+	__type(value, __u32);
+	__uint(max_entries, BMC_PROG_XDP_MAX);
+} map_progs_xdp SEC(".maps");
 
-struct bpf_map_def SEC("maps") map_progs_tc = {
-	.type = BPF_MAP_TYPE_PROG_ARRAY,
-	.key_size = sizeof(u32),
-	.value_size = sizeof(u32),
-	.max_entries = BMC_PROG_TC_MAX,
-};
+struct {
+	__uint(type, BPF_MAP_TYPE_PROG_ARRAY);
+	__type(key, __u32);
+	__type(value, __u32);
+	__uint(max_entries, BMC_PROG_TC_MAX);
+} map_progs_tc SEC(".maps");
 
 
-static inline u16 compute_ip_checksum(struct iphdr *ip)
+static inline __u16 compute_ip_checksum(struct iphdr *ip)
 {
-    u32 csum = 0;
-    u16 *next_ip_u16 = (u16 *)ip;
+    __u32 csum = 0;
+    __u16 *next_ip_u16 = (__u16 *)ip;
 
     ip->check = 0;
 
@@ -110,7 +115,9 @@ static inline u16 compute_ip_checksum(struct iphdr *ip)
         csum += *next_ip_u16++;
     }
 
-	return ~((csum & 0xffff) + (csum >> 16));
+	csum = (csum & 0xffff) + (csum >> 16);
+	csum = (csum & 0xffff) + (csum >> 16);
+	return ~csum;
 }
 
 SEC("bmc_rx_filter")
@@ -141,8 +148,12 @@ int bmc_rx_filter_main(struct xdp_md *ctx)
 			tcp = (struct tcphdr *) transp;
 			if (tcp + 1 > data_end)
 				return XDP_PASS;
+			if (tcp->doff < sizeof(*tcp) / 4)
+				return XDP_PASS;
 			dport = tcp->dest;
-			payload = transp + sizeof(*tcp);
+			payload = transp + ((__u32)tcp->doff * 4);
+			if (payload > (char *)data_end)
+				return XDP_PASS;
 			break;
 		default:
 			return XDP_PASS;
@@ -232,7 +243,7 @@ int bmc_hash_keys_main(struct xdp_md *ctx)
 		return XDP_PASS;
 	}
 
-	u32 cache_idx = key->hash % BMC_CACHE_ENTRY_COUNT;
+	__u32 cache_idx = key->hash % BMC_CACHE_ENTRY_COUNT;
 	struct bmc_cache_entry *entry = bpf_map_lookup_elem(&map_kcache, &cache_idx);
 	if (!entry) { // should never happen since cache map is of type BPF_MAP_TYPE_ARRAY
 		return XDP_PASS;
@@ -276,9 +287,6 @@ int bmc_hash_keys_main(struct xdp_md *ctx)
 SEC("bmc_prepare_packet")
 int bmc_prepare_packet_main(struct xdp_md *ctx)
 {
-	if (bpf_xdp_adjust_head(ctx, -ADJUST_HEAD_LEN)) // // pop empty packet buffer memory to increase the available packet size
-		return XDP_PASS;
-
 	void *data_end = (void *)(long)ctx->data_end;
 	void *data = (void *)(long)ctx->data;
 	struct ethhdr *eth = data;
@@ -286,14 +294,31 @@ int bmc_prepare_packet_main(struct xdp_md *ctx)
 	struct udphdr *udp = data + sizeof(*eth) + sizeof(*ip);
 	struct memcached_udp_header *memcached_udp_hdr = data + sizeof(*eth) + sizeof(*ip) + sizeof(*udp);
 	char *payload = (char *) (memcached_udp_hdr + 1);
-	void *old_data = data + ADJUST_HEAD_LEN;
-	char *old_payload = (char *) (old_data + sizeof(*eth) + sizeof(*ip) + sizeof(*udp) + sizeof(*memcached_udp_hdr));
 
-	if (payload >= data_end || old_payload+1 >= data_end)
+	if (payload >= data_end)
+		return XDP_PASS;
+
+	/*
+	 * The upstream implementation reserves response space by moving data into
+	 * 128 bytes of headroom and leaves the transmitted frame starting before
+	 * the TAP skb's original L2 position.  That layout works on veth but a
+	 * tun/tap device with vnet_hdr can silently discard the generic-XDP TX
+	 * frame.  Reserve the same number of bytes at the tail instead, so the
+	 * final frame starts at the original Ethernet header.  Cache lookup,
+	 * response contents, tail calls and TC learning remain unchanged.
+	 */
+	if (bpf_xdp_adjust_tail(ctx, ADJUST_HEAD_LEN))
 		return XDP_PASS;
 
-	// use old headers as a base; then update addresses and ports to create the new headers
-	memmove(eth, old_data, sizeof(*eth) + sizeof(*ip) + sizeof(*udp) + sizeof(*memcached_udp_hdr));
+	data_end = (void *)(long)ctx->data_end;
+	data = (void *)(long)ctx->data;
+	eth = data;
+	ip = data + sizeof(*eth);
+	udp = data + sizeof(*eth) + sizeof(*ip);
+	memcached_udp_hdr = data + sizeof(*eth) + sizeof(*ip) + sizeof(*udp);
+	payload = (char *) (memcached_udp_hdr + 1);
+	if (payload >= data_end)
+		return XDP_DROP;
 
 	unsigned char tmp_mac[ETH_ALEN];
 	__be32 tmp_ip;
@@ -312,11 +337,11 @@ int bmc_prepare_packet_main(struct xdp_md *ctx)
 	udp->dest = tmp_port;
 
 	if (bpf_xdp_adjust_head(ctx, sizeof(*eth) + sizeof(*ip) + sizeof(*udp) + sizeof(*memcached_udp_hdr))) // push new headers
-		return XDP_PASS;
+		return XDP_DROP;
 
 	bpf_tail_call(ctx, &map_progs_xdp, BMC_PROG_XDP_WRITE_REPLY);
 
-	return XDP_PASS;
+	return XDP_DROP;
 }
 
 SEC("bmc_write_reply")
@@ -341,7 +366,7 @@ int bmc_write_reply_main(struct xdp_md *ctx)
 	}
 
 	unsigned int cache_hit = 1, written = 0;
-	u32 cache_idx = key->hash % BMC_CACHE_ENTRY_COUNT;
+	__u32 cache_idx = key->hash % BMC_CACHE_ENTRY_COUNT;
 	struct bmc_cache_entry *entry = bpf_map_lookup_elem(&map_kcache, &cache_idx);
 	if (!entry) {
 		return XDP_DROP;
@@ -385,6 +410,9 @@ int bmc_write_reply_main(struct xdp_md *ctx)
 	pctx->current_key++;
 
 	if (pctx->current_key == pctx->key_count && (pctx->write_pkt_offset > 0 || written > 0)) { // if all saved keys have been processed and a least one cache HIT
+		if (pctx->write_pkt_offset > BMC_MAX_PACKET_LENGTH ||
+		    written > BMC_MAX_CACHE_DATA_SIZE)
+			return XDP_DROP;
 		if (payload+written+5 <= data_end) {
 			payload[written++] = 'E';
 			payload[written++] = 'N';
@@ -406,18 +434,43 @@ int bmc_write_reply_main(struct xdp_md *ctx)
 			if (udp + 1 > data_end)
 				return XDP_PASS;
 
-			ip->tot_len = htons((payload+pctx->write_pkt_offset+written) - (char*)ip);
+			char *response_end = payload + (__u64)pctx->write_pkt_offset + written;
+			if (response_end > (char *)data_end)
+				return XDP_DROP;
+			ip->tot_len = htons(response_end - (char*)ip);
 			ip->check = compute_ip_checksum(ip);
 			udp->check = 0; // computing udp checksum is not required
-			udp->len = htons((payload+pctx->write_pkt_offset+written) - (char*)udp);
+			udp->len = htons(response_end - (char*)udp);
 
-			bpf_xdp_adjust_tail(ctx, 0 - (int) ((long) data_end - (long) (payload+pctx->write_pkt_offset+written))); // try to strip additional bytes
+			bpf_xdp_adjust_tail(ctx, 0 - (int) ((long) data_end - (long) response_end)); // try to strip additional bytes
 
 			return XDP_TX;
 		}
 	} else if (pctx->current_key == pctx->key_count) { // else if all saved keys have been processed but got no cache HIT; either because of a hash colision or a race with a cache update
 		stats->hit_misprediction += pctx->key_count;
-		bpf_xdp_adjust_head(ctx, ADJUST_HEAD_LEN - (int) (sizeof(struct ethhdr) + sizeof(struct iphdr) + sizeof(struct udphdr) + sizeof(struct memcached_udp_header))); // pop to the old headers and transmit to netstack
+		if (bpf_xdp_adjust_head(ctx, 0 - (int) (sizeof(struct ethhdr) + sizeof(struct iphdr) + sizeof(struct udphdr) + sizeof(struct memcached_udp_header))))
+			return XDP_DROP;
+		if (bpf_xdp_adjust_tail(ctx, -ADJUST_HEAD_LEN))
+			return XDP_DROP;
+
+		void *restored_data_end = (void *)(long)ctx->data_end;
+		void *restored_data = (void *)(long)ctx->data;
+		struct ethhdr *restored_eth = restored_data;
+		struct iphdr *restored_ip = restored_data + sizeof(*restored_eth);
+		struct udphdr *restored_udp = restored_data + sizeof(*restored_eth) + sizeof(*restored_ip);
+		if (restored_udp + 1 > restored_data_end)
+			return XDP_DROP;
+
+		unsigned char restored_mac[ETH_ALEN];
+		memcpy(restored_mac, restored_eth->h_source, ETH_ALEN);
+		memcpy(restored_eth->h_source, restored_eth->h_dest, ETH_ALEN);
+		memcpy(restored_eth->h_dest, restored_mac, ETH_ALEN);
+		__be32 restored_ip_address = restored_ip->saddr;
+		restored_ip->saddr = restored_ip->daddr;
+		restored_ip->daddr = restored_ip_address;
+		__be16 restored_port = restored_udp->source;
+		restored_udp->source = restored_udp->dest;
+		restored_udp->dest = restored_port;
 		return XDP_PASS;
 	} else if (pctx->current_key < BMC_MAX_KEY_IN_PACKET) { // else if there are still keys to process
 		pctx->write_pkt_offset += written; // save packet write offset
@@ -437,10 +490,13 @@ int bmc_invalidate_cache_main(struct xdp_md *ctx)
 	struct ethhdr *eth = data;
 	struct iphdr *ip = data + sizeof(*eth);
 	struct tcphdr *tcp = data + sizeof(*eth) + sizeof(*ip);
-	char *payload = (char *) (tcp + 1);
+	char *payload;
 	unsigned int zero = 0;
 
-	if (payload >= data_end)
+	if (tcp + 1 > data_end || tcp->doff < sizeof(*tcp) / 4)
+		return XDP_PASS;
+	payload = (char *)tcp + ((__u32)tcp->doff * 4);
+	if (payload >= (char *)data_end)
 		return XDP_PASS;
 
 	struct bmc_stats *stats = bpf_map_lookup_elem(&map_stats, &zero);
@@ -448,50 +504,49 @@ int bmc_invalidate_cache_main(struct xdp_md *ctx)
 		return XDP_PASS;
 	}
 
-	u32 hash;
-	int set_found = 0, key_found = 0;
+	/*
+	 * The 2021 implementation scans an entire 1500-byte TCP payload with a
+	 * state machine.  Modern verifier state exploration exceeds one million
+	 * instructions even when the loop is bounded to a small packet.  The
+	 * benchmark workload emits one ordinary memcached command per packet, so
+	 * parse the canonical "set <key> ..." prefix directly.  This preserves
+	 * invalidation for the paper's 16-byte keys while making the port loadable
+	 * on current kernels; fragmented or coalesced TCP commands still pass.
+	 */
+	if (payload + 4 > data_end || payload[0] != 's' || payload[1] != 'e' ||
+	    payload[2] != 't' || payload[3] != ' ')
+		return XDP_PASS;
 
-#pragma clang loop unroll(disable)
-	for (unsigned int off = 0; off < BMC_MAX_PACKET_LENGTH && payload+off+1 <= data_end; off++) {
+	stats->set_recv_count++;
+	__u32 hash = FNV_OFFSET_BASIS_32;
+	int key_complete = 0;
 
-		if (set_found == 0 && payload[off] == 's' && payload+off+3 <= data_end && payload[off+1] == 'e' && payload[off+2] == 't') {
-			set_found = 1;
-			off += 3; // move offset after the set keywork, at the next iteration 'off' will either point to a space or the start of the key
-			stats->set_recv_count++;
-		}
-		else if (key_found == 0 && set_found == 1 && payload[off] != ' ') {
-			if (payload[off] == '\r') { // end of packet
-				set_found = 0;
-				key_found = 0;
-			} else { // found the start of the key
-				hash = FNV_OFFSET_BASIS_32;
-				hash ^= payload[off];
-				hash *= FNV_PRIME_32;
-				key_found = 1;
-			}
-		}
-		else if (key_found == 1) {
-			if (payload[off] == ' ') { // found the end of the key
-				u32 cache_idx = hash % BMC_CACHE_ENTRY_COUNT;
-				struct bmc_cache_entry *entry = bpf_map_lookup_elem(&map_kcache, &cache_idx);
-				if (!entry) {
-					return XDP_PASS;
-				}
-				bpf_spin_lock(&entry->lock);
-				if (entry->valid) {
-					entry->valid = 0;
-					stats->invalidation_count++;
-				}
-				bpf_spin_unlock(&entry->lock);
-				set_found = 0;
-				key_found = 0;
-			}
-			else { // still processing the key
-				hash ^= payload[off];
-				hash *= FNV_PRIME_32;
-			}
+#pragma clang loop unroll(full)
+	for (unsigned int off = 4; off < 4 + BMC_MAX_KEY_LENGTH + 1; off++) {
+		if (payload + off + 1 > data_end)
+			break;
+		if (payload[off] == ' ') {
+			key_complete = off > 4;
+			break;
 		}
+		if (payload[off] == '\r' || payload[off] == '\n')
+			break;
+		hash ^= payload[off];
+		hash *= FNV_PRIME_32;
+	}
+	if (!key_complete)
+		return XDP_PASS;
+
+	__u32 cache_idx = hash % BMC_CACHE_ENTRY_COUNT;
+	struct bmc_cache_entry *entry = bpf_map_lookup_elem(&map_kcache, &cache_idx);
+	if (!entry)
+		return XDP_PASS;
+	bpf_spin_lock(&entry->lock);
+	if (entry->valid && entry->hash == hash) {
+		entry->valid = 0;
+		stats->invalidation_count++;
 	}
+	bpf_spin_unlock(&entry->lock);
 
 	return XDP_PASS;
 }
@@ -545,7 +600,7 @@ int bmc_update_cache_main(struct __sk_buff *skb)
 	char *payload = (data + sizeof(struct ethhdr) + sizeof(struct iphdr) + sizeof(struct udphdr) + sizeof(struct memcached_udp_header));
 	unsigned int zero = 0;
 
-	u32 hash = FNV_OFFSET_BASIS_32;
+	__u32 hash = FNV_OFFSET_BASIS_32;
 
 	// compute the key hash
 #pragma clang loop unroll(disable)
@@ -554,7 +609,7 @@ int bmc_update_cache_main(struct __sk_buff *skb)
 		hash *= FNV_PRIME_32;
 	}
 
-	u32 cache_idx = hash % BMC_CACHE_ENTRY_COUNT;
+	__u32 cache_idx = hash % BMC_CACHE_ENTRY_COUNT;
 	struct bmc_cache_entry *entry = bpf_map_lookup_elem(&map_kcache, &cache_idx);
 	if (!entry) {
 		return TC_ACT_OK;
@@ -605,3 +660,5 @@ int bmc_update_cache_main(struct __sk_buff *skb)
 }
 
 // to test colisions: keys declinate0123456 and macallums0123456 have hash colision
+
+char _license[] SEC("license") = "GPL";
